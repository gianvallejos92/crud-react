@import 'Vars.styl'

.footer
  position: fixed;
  bottom: 0;
  left: 0;  
  width: 100%;
  background: $color-theme;
  color: $color-white;
  margin-top: 40px;
  display: flex;
  justify-content: center
  font-size: .8rem
  height: 50px
  padding: 10px 0px
  .copy
    display: flex
    align-items: center